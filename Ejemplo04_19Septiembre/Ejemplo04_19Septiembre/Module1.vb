Module Module1

    Sub Main()
        'Perdida de precision, error desbordamiento.
        Dim numero As Integer = 10
        Dim raiz As Double = Math.Sqrt(numero)
        Console.WriteLine("La reiz es: " & raiz)
        Console.ReadLine()
    End Sub

End Module
